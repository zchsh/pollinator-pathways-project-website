/**
 * $TSFixMe lets us flag anywhere we're intentionally using
 * the loose `any` type as a stopgap for stronger typing.
 * Ideally, we'd have not $TSFixMe types used in this project...
 * A good maintenance task is to search for `$TSFixMe` in the project
 * and add better types wherever this is used.
 */
type $TSFixMe = any;
